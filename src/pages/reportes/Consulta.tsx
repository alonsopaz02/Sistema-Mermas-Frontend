import React from "react";
import { Box, Typography } from "@mui/material";

const Consulta: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Pagina de consultas</Typography>
      <Typography variant="body1">Aquí podrás administrar las estaciones. (En desarrollo)</Typography>
    </Box>
  );
};

export default Consulta;
